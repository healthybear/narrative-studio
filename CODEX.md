# Narrative Studio Codex 规则

本文件定义 Codex 在本仓库中的默认执行规则。目标是减少协作偏差，保持实现、文档和代码风格一致。

## 1. 沟通与文档

- 与用户的交流默认使用中文。
- 新增或修改的代码注释默认使用中文。
- 新增或修改的项目文档、README、说明文件默认使用中文。
- 回答时优先说明：
  1. 做了什么
  2. 如何验证
  3. 还有哪些边界或风险

## 2. 项目现状优先于文档描述

- 先读当前仓库代码，再做实现，不凭历史文档直接假设。
- 当文档与代码冲突时，优先以当前仓库代码为准。
- 当前前端实际栈以 `apps/web/package.json` 为准：
  - `nuxt@4.4.5`
  - `vue@3.5.x`
  - `typescript`
- 不要因为 `CLAUDE.md` 中写了 Nuxt 3 就回退到旧写法。

## 3. 技术栈约束

### 前端

- 保持 `Nuxt + Vue + TypeScript` 现有模式。
- 默认使用 Vue Composition API。
- Vue 单文件组件优先使用 `<script setup lang="ts">`。
- 新增前端逻辑时，优先沿用现有 store / utils / page 结构，不随意引入新的状态管理模式。

### 后端

- 保持 `Fastify + TypeScript` 现有模式。
- 只有在用户明确要求或当前任务确实需要时，才扩展后端 API。

### NLP

- NLP 相关能力默认遵循仓库现有路线：
  - Python 3.10+
  - spaCy
  - NLTK

## 4. 工程与依赖管理

- 包管理器统一使用 `pnpm`。
- Monorepo 任务优先通过 `turbo` 或 `pnpm --filter` 执行。
- 不引入与当前任务无关的大型依赖。
- 新增依赖前先判断是否能用现有依赖完成。

## 5. 代码风格

- TypeScript 保持严格模式，优先修正类型，而不是绕过类型系统。
- 代码倾向简单、直接、低抽象。
- 避免为了“看起来更通用”而提前抽象。
- 注释要解释“为什么这样做”，不要只复述代码行为。
- 对复杂文件，保留简短文件头注释说明用途。
- 函数、工具和关键数据转换逻辑应有中文注释。

## 6. 数据模型与命名

- 优先保持核心模型语义稳定：
  - `Novel`
  - `Chapter`
  - `Scene`
  - `Character`
  - `NarrativeAnalysis`
- 不随意重命名核心概念。
- 如果必须调整核心命名，需同步检查：
  - 类型定义
  - store
  - IndexedDB / API
  - 页面引用
  - 文档说明

## 7. 实现流程

- 先建立上下文，再开始修改。
- 涉及行为变化的任务，优先补可验证测试。
- 至少保证改动路径可以通过以下一种方式验证：
  - 自动化测试
  - 类型检查
  - 本地构建
  - 可复现的页面操作路径
- 改动完成后，优先运行与改动最相关的最小验证集。

## 8. Git 与工作区约束

- 不回滚用户未明确要求撤销的更改。
- 不覆盖与当前任务无关的现有改动。
- 提交信息风格尽量对齐仓库约定：
  - `feat`
  - `fix`
  - `docs`
  - `refactor`
  - `test`
  - `chore`

## 9. 适用于本仓库的执行偏好

- 前端功能优先补齐真实数据流，不堆临时 mock 状态。
- 数据采集、解析、存储类需求，优先把：
  - 类型
  - 存储层
  - store
  - 页面层
  串成一条闭环。
- 纯前端阶段，不强行引入后端依赖。
- 若仓库已有可工作的基础设施，优先在现有基础上补完，而不是重搭一套。

## 10. 完成定义

一个任务默认在以下条件满足后才算完成：

- 代码已实现
- 关键路径已验证
- 用户可见结果已接入实际页面或实际调用链
- 最终说明中明确写出验证结果与剩余边界
